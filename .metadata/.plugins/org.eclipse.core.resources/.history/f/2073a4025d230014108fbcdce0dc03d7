package action;

import javax.servlet.http.HttpServletRequest;

import org.apache.struts2.ServletActionContext;

import com.opensymphony.xwork2.ActionSupport;

import domain.ProjectInfo;

public class GetManageProjectInfoAction extends ActionSupport{

	/**
	 * 
	 */
	private static final long serialVersionUID = 1L;
	
	private ProjectService projectService;
	public String execute(){
		HttpServletRequest request = ServletActionContext.getRequest();
		String s = request.getParameter("projectId");
		ProjectInfo projectInfo = projectService.selectProjectInfo(Integer.parseInt(s)); 
		if(projectInfo != null){
			return SUCCESS;
		}
		else{
			return ERROR;
		}
	}
}
